class Todo {
  constructor(title, desc, priority, dueDate) {
    this.title = title;
    this.desc = desc;
    this.project;
    this.priority = priority;
    this.dueDate = dueDate;
    this.createdDate;
  }
}